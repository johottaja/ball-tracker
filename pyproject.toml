[project]
name = "balltracker"
version = "0.1.0"
description = "Webcam recorder and frame-by-frame viewer with ball detection"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "opencv-python>=4.10",
    "pillow>=11.0",
    "ultralytics>=8.4.62",
]
